pipeline{
   
   agent{ node { label "master" } }
   
   stages{
     stage('bulid'){
       steps{
         script{
           mvnHome = "M2"
           sh "${mvnHome}/bin/mvn -v"
         }
       }
     }
   }
}
